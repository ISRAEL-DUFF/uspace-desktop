
declare global {
    interface Window {
        uspace: {
            versions: any,
            socket: any,
            clipboard: any,
        };
    }
}

export default function systemFunction() {

}